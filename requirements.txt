Django==6.0.1
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
Pillow==12.1.0
python-dotenv==1.2.2
dj-database-url==3.1.2
qrcode==8.2
cloudinary==1.44.2

# Google Sign-In (accounts/google_oauth.py): PyJWT verifies the ID token's
# signature against Google's rotating public keys (fetched over `requests`,
# cached in-process — see PyJWKClient). Both already installed transitively
# (via djangorestframework-simplejwt and cloudinary respectively); pinned
# explicitly here now that accounts/ imports them directly.
PyJWT==2.12.1
requests==2.33.1

# Production server + Render-specific plumbing. Harmless locally — gunicorn
# and whitenoise are simply never invoked by `manage.py runserver`, and
# psycopg2-binary/cloudinary only activate when DATABASE_URL/CLOUDINARY_URL
# are actually set (see config/settings.py).
gunicorn==25.1.0
whitenoise==6.12.0
psycopg2-binary==2.9.11

# Optional: persistent media storage on Render (see config/settings.py's
# CLOUDINARY_URL handling). Unused, at zero cost, until that env var is set.
django-cloudinary-storage==0.3.0
