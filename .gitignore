# Environment / secrets — never commit real values, only .env.example
.env

# Local SQLite database
db.sqlite3
db.sqlite3-journal

# Build artifacts — regenerated by build.sh on every Render deploy
staticfiles/

# User-uploaded media (local dev only — Render uses CLOUDINARY_URL instead)
media/

# Python
__pycache__/
*.pyc
*.pyo
.Python

# Virtual environments
venv/
env/
.venv/

# Editor/OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db
