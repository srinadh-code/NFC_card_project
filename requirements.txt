Django==6.0.1
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
Pillow==12.1.0
python-dotenv==1.2.2
dj-database-url==3.1.2
qrcode==8.2
cloudinary==1.44.2

# `requests` is used directly in two places: accounts/emails.py's
# send_password_reset_otp_email() (Brevo's transactional-email HTTP API) and
# accounts/google_oauth.py's PyJWT PyJWKClient (fetches/caches Google's
# public signing keys to verify Google Sign-In ID tokens). PyJWT itself was
# already installed transitively via djangorestframework-simplejwt; pinned
# explicitly here now that accounts/ imports both directly.
PyJWT==2.12.1
# PyJWT's RS256 verification (used for Google's ID tokens — Google always
# signs with RS256, never HS256) needs this to be installed too; simplejwt's
# own tokens use HS256 and never needed it, which is why this was missing
# until accounts/google_oauth.py started verifying real RS256 signatures.
cryptography==44.0.0
requests==2.33.1

# Production server + Render-specific plumbing. Harmless locally — gunicorn
# and whitenoise are simply never invoked by `manage.py runserver`, and
# psycopg2-binary/cloudinary only activate when DATABASE_URL/CLOUDINARY_URL
# are actually set (see config/settings.py).
gunicorn==25.1.0
whitenoise==6.12.0
psycopg2-binary==2.9.11

# Optional: persistent media storage on Render (see config/settings.py's
# CLOUDINARY_URL handling). Unused, at zero cost, until that env var is set.
django-cloudinary-storage==0.3.0
