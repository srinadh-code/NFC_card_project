# Django
SECRET_KEY=change-me-to-a-random-secret-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database. Leave unset to use local SQLite (dev default).
# For Postgres in production:
# DATABASE_URL=postgres://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME
DATABASE_URL=

# JWT
ACCESS_TOKEN_LIFETIME_MINUTES=30
REFRESH_TOKEN_LIFETIME_DAYS=7

# Email. Leave EMAIL_HOST unset to use the console backend (prints emails to
# the server log) which is fine for local development.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=no-reply@vrsnexora.com

# Frontend
# CORS_ALLOWED_ORIGINS lists every origin Vite might actually be serving on.
# Vite auto-bumps to 5174/5175/... whenever a previous dev server is still
# holding 5173 (a stray process, another terminal, etc.) instead of failing -
# and a port that's missing here gets silently CORS-blocked, which shows up
# in the app as a generic "Something went wrong" with no clear cause. Keep
# this list a step ahead of that.
FRONTEND_URL=http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:5175

# Google OAuth (leave blank to keep the /api/auth/google/ endpoint disabled)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=


